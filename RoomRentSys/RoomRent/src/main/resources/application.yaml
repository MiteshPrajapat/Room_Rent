spring:
  application:
    name: RoomRent

  datasource:
    url: jdbc:mysql://localhost:3306/room_rent
    username: root
    password: mitesh6805
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8081
